 AMPQ - advanced message queuing protocol
 AMPQ имеет 3 базовые понятия:
 1)обменник
 2)очередь
 - name
 - durable
 - Exclusive
 - Auto-delete
 - Arguments
 3)связь или маршрут(routing key)
                        routing key
 Publisher -> exchange -------------> queue -> consumer
 Простыми словами:
 Отправитель -> Сотрудник почтового отедления(маршрутизатор) -> Абонентский ящик -> Получатель


 AMQP – один из наиболее распространенных на сегодняшний день протоколов обмена сообщениями между компонентами распределённой системы. Главной отличительной особенностью данного протокола является концепция построения маршрута сообщения, содержащая два основных структурных элемента: очередь(queue) и точка обмена (exchange). Очередь накапливает в себе сообщения до момента их получения. Точка обмена представляет собой распределитель сообщений, который направляет их либо в нужную очередь, либо в другую точку обмена. Правила распределения (bindings), по которым точка обмена определяет куда именно направить сообщение, основаны на проверке ключа маршрутизации сообщения (routing key) на соответствие заданной маске.


Типы exchange:
1) Direct
2) Topic
3) Fanout
4) Headers
Могут расширяться с помощью плагинов



3.  Публикация/Подписка

Producer(Производитель) — это пользовательское приложение, которое отправляет сообщения .
Queue(Очередь) — это буфер, в котором хранятся сообщения .
Consumer(Потребитель) — это пользовательское приложение, которое получает сообщения .

Зачастую производитель даже не знает, будет ли сообщение вообще доставлено в какую-либо очередь. Вместо этого производитель может только отправлять сообщения на exchange(Биржа). Биржа(обмена) должна точно знать, что делать с полученным сообщением. Должен ли он быть добавлен в определенную очередь? Следует ли добавлять его во многие очереди? Или его следует выбросить. Правила для этого определяются exchange type.
Доступно несколько типов обмена: direct, topic, headers и fanout. Мы сосредоточимся на последнем — fanout.


Временные очереди:
